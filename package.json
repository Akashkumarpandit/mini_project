{
  "name": "rental-place-project",
  "version": "1.0.0",
  "description": "Rental Place Platform - Backend and Frontend",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "backend": "mvn spring-boot:run --file auth-backend/pom.xml",
    "frontend": "npm run dev --prefix frontend",
    "install-all": "npm install --prefix frontend"
  }
}
